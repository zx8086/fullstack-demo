/**
 * Using https://github.com/BridgeAR/safe-stable-stringify because it's simply more robust and more performant!
 * copy here as the esm version is not working in dev & SSR. (Probably a vite issue?)
 * Keeping the dependency to the package anyway.
 */
declare const stringify: (value: any, replacer: any, space: any) => any;
export { configure, stringify };
declare function configure(options: any): (value: any, replacer: any, space: any) => any;
